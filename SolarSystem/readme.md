1. In the main method in System.py you need to set the path to the path of the file 
2. The file must contain inputs in order of, mass, orbital raidus, size, name and colour
3. Don't end end the file in a new line, it will try to read that new line as well
4. Commented out the input i values i used below main function call
5. Timestep is set to 100,000 you can change it if you want in Body.py
6. attached the input.txt that i used as well
7. it'll create a new file called energy.txt when you run it, you can change the frequency of the times you want to print it, set at 500 currently
